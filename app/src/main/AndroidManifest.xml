<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="gaddet_bazaar.challengeaccepted"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Challenge"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ChallengeListActivity"
            android:label="@string/app_name"
            android:parentActivityName=".Challenge">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="gaddet_bazaar.challengeaccepted.Challenge"/>
        </activity>

        <activity
            android:name=".UserActions.SignUp"
            android:label="@string/app_name"
            android:parentActivityName=".UserActions.Login">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="gaddet_bazaar.challengeaccepted.Login"/>
        </activity>
        <activity
            android:name=".MyActivity"
            android:label="@string/title_activity_my">
        </activity>

        <activity
            android:name=".UserActions.Login"
            android:label="@string/title_activity_login">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="gaddet_bazaar.challengeaccepted.Challenge"/>
        </activity>
    </application>

</manifest>
